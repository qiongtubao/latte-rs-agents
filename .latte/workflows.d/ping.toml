# Workflow: ping
# 最简 workflow 验证整条命令触发管线。
name = "ping"
command = "/ping"
description = "测试 workflow 命令触发（Echo）"
max_rounds = 1

[[steps]]
id = "echo"
description = "回复 Pong"
speakers = ["architect"]
prompt = "回复：Pong! 你说了 '{{topic}}'"
