{
  "name": "latte-agent-ui",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port ${VITE_PORT:-5173}",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "self-loop": "tsx self-loop/runner.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.4.0",
    "vite": "^5.4.0",
    "vitest": "^1.6.0",
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "playwright": "^1.44.0",
    "@playwright/test": "^1.44.0"
  }
}
